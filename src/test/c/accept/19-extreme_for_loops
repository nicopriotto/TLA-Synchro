// Complex for loop patterns
int main() {
    // Multiple variable declarations in for loop
    for (int i = 0, int j = 10; i < j; i++, j--) {
        //print(i + j);
        
        // Nested for with complex conditions
        for (int k = i; k < (i + j) and k < 20; k += 2) {
            if (k % 3 == 0) {
                for (int m = 0; m < k and m < 5; m++) {
                    //print(m * k);
                }
            }
        }
    }
    
    // For loop with complex initialization and update
    for (int x = 1, int y = 100; (x * y) > 50 and x < 20; x *= 2, y /= 2) {
        //print(x * y);
    }
    
    return 0;
}
