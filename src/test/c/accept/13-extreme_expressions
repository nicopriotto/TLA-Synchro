// Complex arithmetic && logical expressions
int main() {
    int a = 1;
    int b = 2;
    int c = 3;
    float f = 1.5;
    boolean result = true;
    
    // Complex arithmetic
    int complex = ((a + b) * c - (a % b)) / (c + 1) + (++a * --b);
    
    // Complex logical expressions
    boolean blabla = 1 < (20 - 20) * (10 - 20) and 0 < 20;
    
    // Mixed expressions with function calls
    if (((a++ + ++b) * (c-- - --a))) {
        print(result);
    }
    
    // Chained comparisons
    if (a < b && b < c && c < (a + b + c)) {
        print(1);
    }
    
    //return complex;
    return 0;
}
