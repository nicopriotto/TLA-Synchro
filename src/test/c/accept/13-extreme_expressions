int main() {
    int a = 1;
    int b = 2;
    int c = 3;
    float f = 1.5;
    boolean result = true;
    
    int complex = ((a + b) * c - (a % b)) / (c + 1) + (++a * --b);
    
    boolean blabla = 1 < (20 - 20) * (10 - 20) and 0 < 20;
    
    if (((a++ + ++b) * (c-- - --a))) {
        print(result);
    }
    
    if (a < b && b < c && c < (a + b + c)) {
        print(1);
    }
    
    return 0;
}
