sem mutex = 1;
int sleepTime = 1;

int A() {
   forever {
        down(mutex);
        print("ACCESO SECCIÓN CRÍTICA - A");
        sleep(sleepTime);
        up(mutex);
        sleep(sleepTime);
    }
}

int B() {
    forever {
        down(mutex);
        print("ACCESO SECCIÓN CRÍTICA - B");
        sleep(sleepTime);
        up(mutex);
        sleep(sleepTime);
    }
}

int main() {
    int a = 2;
    thread(A, a);   
    thread(B, 5);
    forever {}
    return 0;
}

