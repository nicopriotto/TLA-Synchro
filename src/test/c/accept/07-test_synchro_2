sem mutex = 1;

int A() {
    while(1 == 1) {
        down(mutex);
        print("ACCESO SECCION CRITICA - A");
        up(mutex);
    }
}

int B() {
    while(1 == 1) {
        down(mutex);
        print("ACCESO SECCION CRITICA - B");
        up(mutex);
    }
}

int main() {
    thread(A, 2);   
    thread(B, 5);
    return status;
}
