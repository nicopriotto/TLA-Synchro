sem mutex = 1;

int A() {
    while(2 > 1) {
        down(mutex);
        print("ACCESO SECCION CRITICA - A");
        up(mutex);
    }
}

int B() {
    while(2 > 1) {
        down(mutex);
        print("ACCESO SECCION CRITICA - B");
        up(mutex);
    }
}

int main() {
    int a = 0;
    thread(A, 2);   
    thread(B, 5);
    return 0;
}
