sem resource1 = 3;
sem resource2 = 2;
sem mutex_lock = 1;
int shared_data = 0;

int producer() {
    for (int i = 0; i < 10; i++) {
        down();
        down();
        down();
        
        shared_data++;
        print(shared_data);
        sleep(1);
        
        up();
        up();
        up();
    }
    return shared_data;
}

int consumer() {
    while (shared_data < 50) {
        down();
        
        if (shared_data > 0) {
            shared_data--;
            print(shared_data);
        }
        
        up();
        sleep(2);
    }
    return 0;
}

int main() {
    thread(producer);
    thread(producer);
    thread(consumer);
    
    sleep(30);
    
    return shared_data;
}
