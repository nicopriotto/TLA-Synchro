sem invalid_sem;

int main() {
    sem local_sem = -1;
    
    up(5);
    down("invalid");
    
    local_sem = local_sem + 1;
    local_sem = local_sem * 2;
    
    if (local_sem > invalid_sem) {
        print(1);
    }
    
    int x = local_sem;
    local_sem = 10;
    
    return 0;
}
